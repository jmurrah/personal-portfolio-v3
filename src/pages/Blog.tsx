import BlogContent from '@/components/Blog/BlogContent';

export default function Blog() {
  return (
    <section>
      <h1 className="mono-heading mb-6 text-4xl font-semibold">Writing</h1>
      <BlogContent />
    </section>
  );
}
